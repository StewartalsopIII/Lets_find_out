/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['replicate.delivery'],
      domains: ['yquazwjgvgcderqopaec.supabase.co'],
    },
}

export default nextConfig;